FROM node:18-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install

COPY . .

# Build not required for dev, but good for prod.
# For this assignment, we will run in dev mode to keep it simple/fast.
CMD ["npm", "run", "dev"]
